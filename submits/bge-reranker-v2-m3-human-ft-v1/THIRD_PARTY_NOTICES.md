# Third-party notice

The fine-tuned checkpoint is derived from `BAAI/bge-reranker-v2-m3`.

The upstream model repository declares the Apache License 2.0:
<https://huggingface.co/BAAI/bge-reranker-v2-m3>.

The submission keeps this notice alongside the derived weights. The exact
fine-tuned artifact is identified by SHA-256 in `SUBMISSION_MANIFEST.json`.
