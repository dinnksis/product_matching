# MiniLM S2 cheap ensemble submission

Meta-model: `catboost`. The frozen S2 score is augmented with symmetric lexical, numeric, product-code and attribute-comparison features.

Build the shared runtime image from `docker/minilm-s2-ensemble-runtime/Dockerfile`.
